import earthkit.regrid

# Reset a named config option to its default value
earthkit.regrid.config.reset("user-cache-directory")

# Reset all the config options to their default values
earthkit.regrid.config.reset()
